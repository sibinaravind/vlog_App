import "package:flutter/material.dart";

class SingleBlog extends StatelessWidget {
  const SingleBlog({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Its empty now")),
    );
  }
}
